{
  "engines": {
    "node": ">=20.6.0"
  },
  "name": "pixels_better_search",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "tsc",
    "dev": "node --env-file=.env --watch -r ts-node/register src/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Zaid Hasan",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.15.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
